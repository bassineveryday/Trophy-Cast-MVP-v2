#!/usr/bin/env bash

# Pre-push guardrails (lightweight locally). Full test suite runs in CI.
npm run lint || exit 1

# Only run live DB checks if envs present
if [ -n "$SUPABASE_URL" ] && [ -n "$SUPABASE_ANON_KEY" ]; then
  npm run beta:aoy-audit || exit 1
fi
