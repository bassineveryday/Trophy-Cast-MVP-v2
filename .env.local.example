# Local environment variables for Trophy Cast (copy to .env.local and fill in your real values)


# Supabase public config (required for real backend)
# Replace the placeholders below with your project's values. Copy this file to `.env.local` and
# fill in the real credentials (do NOT commit `.env.local` to version control).
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional
SENTRY_DSN=your_sentry_dsn_here
NODE_ENV=development
